import VerticalMarquee from "@/components/VerticalMarquee";

export const metadata = {
  title: "Vertical Marquee — TopStake",
  description: "Infinite vertical scrolling text effect",
};

export default function MarqueePage() {
  return (
    <main>
      <VerticalMarquee />
    </main>
  );
}
